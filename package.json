{
  "name": "@souenzzo/vlop",
  "main": "src/index.mjs",
  "scripts": {
    "ci": "npm --audit=false --ignore-scripts=true --update-notifier=false --fund=false --loglevel=error ci",
    "standard": "standard",
    "tsc": "tsc",
    "publish": "npm publish --provenance --access public",
    "test": "node --test"
  },
  "workspaces": [
    "."
  ],
  "devDependencies": {
    "standard": "17.1.2",
    "typescript": "5.7.3"
  }
}
