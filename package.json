{
  "name": "@vlop/index",
  "main": "src/index.mjs",
  "scripts": {
    "ci": "npm --audit=false --ignore-scripts=true --update-notifier=false --fund=false ci",
    "standard": "standard",
    "test": "node --test"
  },
  "workspaces": [
    "."
  ],
  "devDependencies": {
    "standard": "17.1.2"
  }
}
